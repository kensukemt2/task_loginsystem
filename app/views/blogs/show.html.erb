<p id="notice"><%= notice %></p>
<h1>ブログ詳細画面</h1>
<p>タイトル：<%= @blog.title %></p>

<p>内容:<%= @blog.content %></p>

<p>画像:<%= image_tag @blog.image %></p>

<% unless @blog.user == current_user %>
  <% if @favorite.present? %>
    <%= link_to 'お気に入り解除する', favorite_path(blog_id: @blog.id), method: :delete, class: 'btn btn-danger' %>
  <% else %>
    <%= link_to 'お気に入りする', favorites_path(blog_id: @blog.id), method: :post, class: 'btn btn-primary' %>
  <% end %>
  <% else %>
  <%= link_to 'ブログ編集画面へ', edit_blog_path(@blog),class: 'btn btn-success' %>
<% end %>

<%= link_to 'ブログ一覧に戻る', blogs_path,class: 'btn btn-info' %>
